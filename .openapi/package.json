{
  "name": "openapi",
  "version": "3.0.0",
  "description": "openapi",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"./src/**/*.yml\"",
    "lint": "redocly lint \"./src/**/*.yml\"",
    "merge": "swagger-merger -i src/v1.yml -o src/specs/v1.yml"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@redocly/cli": "^1.13.0",
    "prettier": "^3.2.5",
    "swagger-merger": "^1.5.4",
    "watch-cli": "^0.2.3"
  }
}
