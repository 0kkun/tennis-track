description: レコードを新規作成した場合
content:
  application/json:
    schema:
      allOf:
        - type: object
          properties:
            data:
              type: object
          required:
            - data
        - $ref: "#/components/schemas/commonResponse"
    examples:
      success:
        value:
          data: {}
          status: 201
          message: success
