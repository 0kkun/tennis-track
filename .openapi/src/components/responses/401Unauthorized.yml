description: 未認証
content:
  application/json:
    schema:
      allOf:
        - type: object
          properties:
            data:
              type: object
          required:
            - data
        - $ref: "#/components/schemas/commonResponse"
    examples:
      Unauthorized:
        value:
          data:
            errors:
              email: emailは必須です
              password: パスワードは必須です
          status: 401
          message: Unauthorized error.
