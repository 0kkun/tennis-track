title: user
type: object
description: ユーザー情報
properties:
  id:
    $ref: "#/components/schemas/userId"
  name:
    $ref: "#/components/schemas/userName"
  email:
    $ref: "#/components/schemas/userEmail"
