description: 未認証
content:
  application/json:
    schema:
      allOf:
        - type: object
          properties:
            data:
              type: object
          required:
            - data
        - $ref: '../schemas/Response.yaml'
    examples:
      Unauthorized:
        value:
          data:
            password: パスワードが不正です。
          status: 401
          message: Unauthorized error.